{
  "name": "@qloo/openapi-data-validator",
  "version": "0.3.0",
  "description": "Validate, coerce types, fill defaults for API requests using OpenAPI 3.0",
  "main": "build/index.js",
  "files": [
    "build"
  ],
  "typings": "build/index.d.ts",
  "scripts": {
    "build": "rm -rf build/ && npx tsc && echo 'done building'",
    "release": "npm publish --registry=https://npm.pkg.github.com/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/qloo/openapi-data-validator.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "author": "Qloo",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^11.9.3",
    "ajv": "^8.17.1",
    "ajv-errors": "^3.0.0",
    "ajv-formats": "^3.0.1"
  }
}
